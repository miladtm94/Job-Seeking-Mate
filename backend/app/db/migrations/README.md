# Migrations

Use Alembic for schema migrations in production.

Suggested next step:
- add `alembic.ini` and migration scripts under this directory
- wire migration commands into CI and release workflows
